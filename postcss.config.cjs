/**
 * postcss.config.cjs
 * 
 * PostCSS configuration for CSS processing.
 * Configures autoprefixer for browser compatibility.
 * Ensures cross-browser CSS support for the portfolio site.
 */

module.exports = {
  plugins: {
    autoprefixer: {}
  }
};
